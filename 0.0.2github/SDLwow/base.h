
typedef unsigned int coord;

	const coord chunksize=16;
	const coord chunkx=4,chunky=4;

	const short unsigned int SCREEN_WIDTH = 1280;
const short unsigned int SCREEN_HEIGHT = 1024;//800;


const coord LevelSizeX = chunkx*chunksize;
const coord LevelSizeZ=chunky*chunksize;

	const coord EngineLevelSizeX = chunkx*chunksize+4; const coord EngineLevelSizeZ=chunky*chunksize+4;